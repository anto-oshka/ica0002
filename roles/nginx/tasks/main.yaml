---
- name: Install Nginx
  apt:
    name: nginx
    state: latest

- name: Run Nginx
  service:
    name: nginx
    state: started
    enabled: yes

- name: Modify Nginx sites-enabled file
  template:
    src: default.j2
    dest: /etc/nginx/sites-enabled/default
  notify: Restart Nginx
