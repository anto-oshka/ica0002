apiVersion: 1

datasources:
 - name: Prometheus
   type: prometheus
   access: proxy
   orgId: 1
   {% for vm in groups['logging_servers'] -%}
   url: http://{{ vm }}:9090/prometheus
   {% endfor -%}
   isDefault: true
   editable: true
