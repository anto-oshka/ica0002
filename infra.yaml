---
  - name: Init
    hosts: all
    become: yes
    roles:
      - init

  - name: Database Server
    hosts: db_servers
    become: yes
    roles:
      - mysql

  - name: Web Server
    hosts: web_servers
    become: yes
    roles:
      - agama
      - uwsgi
      - nginx
